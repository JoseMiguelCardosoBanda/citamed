<p id="notice"><%= notice %></p>

    <h1 style="text-align: center;">Doctors</h1>

    <table class="table table-striped">
      <thead>
      <tr>
        <th>Nombre</th>
        <th>Especialidad</th>
        <th>No. de cédula</th>
        <th>Consultorio</th>
        <th colspan="3"></th>
      </tr>
      </thead>

      <tbody>
      <% @doctors.each do |doctor| %>
        <tr>
          <td><%= doctor.nombre %></td>
          <td><%= doctor.especialidad %></td>
          <td><%= doctor.no_cedula %></td>
          <td><%= doctor.consultorio %></td>
          <td><%= link_to 'Ver', doctor, :class => "btn btn-info btn-sm" %></td>
          <td><%= link_to 'Editar', edit_doctor_path(doctor), :class => "btn btn-light btn-sm" %></td>
          <td><%= button_to "Eliminar", doctor, method: :delete, :class => "btn btn-danger btn-sm"%></td>
        </tr>
      <% end %>
      </tbody>
    </table>

    <br>


<%= link_to 'Nuevo Doctor', new_doctor_path %>
