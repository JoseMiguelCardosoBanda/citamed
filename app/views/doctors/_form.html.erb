<%= form_with(model: doctor, local: true) do |form| %>
  <% if doctor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(doctor.errors.count, "error") %> prohibited this doctor from being saved:</h2>

      <ul>
        <% doctor.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_field :nombre %>
  </div>

  <div class="field">
    <%= form.label :especialidad %>
    <%= form.text_field :especialidad %>
  </div>

  <div class="field">
    <%= form.label :no_cedula %>
    <%= form.text_field :no_cedula %>
  </div>

  <div class="field">
    <%= form.label :consultorio %>
    <%= form.number_field :consultorio %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
