<%= form_with(model: doctor, local: true) do |form| %>
  <% if doctor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(doctor.errors.count, "error") %> prohibited this doctor from being saved:</h2>

      <ul>
        <% doctor.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-floating">
    <input class="form-control" placeholder="nombre" type="text" name="doctor[nombre]" id="doctor_nombre" class="form-control">
    <label for="floatingInput">Nombre</label>
  </div>

  <div class="form-floating">
    <input class="form-control" placeholder="especialidad" type="text" name="doctor[especialidad]" id="doctor_especialidad" class="form-control">
    <label for="floatingInput">Especialidad</label>
  </div>

  <div class="form-floating">
    <input class="form-control" placeholder="No_cedula" type="text" name="doctor[no_cedula]" id="doctor_no_cedula" class="form-control">
    <label for="floatingInput">No Cedula</label>
  </div>

  <div class="form-floating">
    <input class="form-control" placeholder="Consultorio" type="number" name="doctor[consultorio]" id="doctor_consultorio" class="form-control">
    <label for="floatingInput">Consultorio</label>
  </div>

  <div class="actions">
    <input class="w-100 btn btn-lg btn-primary" type="submit" name="commit" value="Crear Doctor" data-disable-with="Crear Doctor">
  </div>

<% end %>
